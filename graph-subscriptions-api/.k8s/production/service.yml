apiVersion: apps/v1
kind: Deployment
metadata:
  name: graph-subscriptions-api
spec:
  replicas: 5
  selector:
    matchLabels:
      app: graph-subscriptions-api
  template:
    spec:
      containers:
        - name: app
