on:
  push:
    branches: ['**']

jobs:
  test-subgraph:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: yarn
        working-directory: subgraph
      - run: yarn test
        working-directory: subgraph
