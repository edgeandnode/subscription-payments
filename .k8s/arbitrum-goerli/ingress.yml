apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: graph-subscriptions-api-arbitrum-goerli
spec:
  ingressClassName: nginx
  rules:
    - host: api.subscriptions.testnet.thegraph.com
      http:
        paths:
          - backend:
              service:
                name: graph-subscriptions-api-arbitrum-goerli
                port:
                  name: http
            path: /graphql
            pathType: Prefix
